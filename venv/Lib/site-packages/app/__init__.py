from flask import Flask

def hello():
	return 'Hello world!'
def add(x,y):
	return x+y

def create_app(config=None):

    app = Flask(__name__)
    if config is not None:
        app.config.from_pyfile(config)
    # configure your app...
    return app